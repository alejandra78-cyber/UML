package com.modelcollab.metamodel.model;

/**
 * Tipos de dato soportados para atributos del modelo canonico (mapean a tipos SQL/JPA).
 */
public enum AttributeType {
    INTEGER,
    BIGINT,
    VARCHAR,
    TEXT,
    DECIMAL,
    BOOLEAN,
    DATE,
    DATETIME,
    UUID
}

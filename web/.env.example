# URL base del backend (sin barra final ni sufijo de path -- src/config.ts
# le agrega /api/v1, /api/v1/auth, /ws-stomp según cada caso). Copiá este
# archivo a .env y ajustá el valor si tu backend corre en otro host/puerto.
VITE_API_URL=http://localhost:8080
